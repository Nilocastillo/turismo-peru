---
const { className = "" } = Astro.props; // opcional para estilos extra
---

<div
  class={` bg-white text-secondary border border-primary/20 flex gap-2 items-center max-w-max px-4 py-2 rounded-full text-sm ${className}`}
>
  <div class="bg-primary aspect-square w-2 h-2 rounded-full"></div>

  <slot />
  <!-- Aquí va el texto dinámico -->
</div>
